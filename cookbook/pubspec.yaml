name: cookbook
description: "A new Flutter project."
publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  animations: ^2.0.10
  cupertino_icons: ^1.0.6
  faker: ^2.1.0
  flutter:
    sdk: flutter
  go_router: ^12.1.3

  smooth_sheets:
    path: ../package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
